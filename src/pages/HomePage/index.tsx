import React from 'react';

const HomePage = () => {
  // Build a basic homepage
  const one = 1;
  //   eslint-disable-next-line
  console.log(one);
  return (
    <div>
      <h1>Home Page</h1>
    </div>
  );
};

export default HomePage;
